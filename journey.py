import time
from colors import green, grey
from objects import Game, Inventory, Person, Player
from utils import clear, LINE, CENT
from validators import (
    validate_choice, validate_yes_no, validate_minmax
)


PLAYER = GAME = INVENTORY = None


def landmark_menu():
    """
    Generic function that loads the landmark-specific menu options
    """
    pass
